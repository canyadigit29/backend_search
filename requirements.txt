aiofiles==24.1.0
fastapi==0.115.0
# httpx 0.25.x lacks the 'proxy' parameter used internally by current supabase/gotrue client.
# Bump to >=0.27 where 'proxy' is supported; pin to a concrete version for reproducibility.
httpx==0.28.1
# Pin OpenAI SDK to the version present in the local openai-python workspace for API parity.
openai==2.7.1
psycopg2-binary==2.9.9
pydantic==2.9.2
python-dotenv==1.0.1
python-multipart==0.0.9
# OCR: we overwrite PDFs with OCRmyPDF; pypdf handles text extraction. Drop unused pytesseract/pdf2image.
ocrmypdf==16.0.3
requests==2.32.3
# Supabase Python client
supabase==2.4.0
uvicorn==0.30.6
numpy==1.26.4
tiktoken==0.7.0
# Keep only pypdf for PDF text extraction in the worker
pypdf==5.1.0
# Google APIs for Drive sync
google-api-python-client==2.146.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.0




